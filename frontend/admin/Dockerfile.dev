# 建材通管理后台 - 开发镜像 (Vite 热更新)
FROM node:20-alpine

WORKDIR /app
RUN corepack enable && corepack prepare pnpm@9 --activate

COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile || pnpm install

COPY . .

EXPOSE 4002

CMD ["pnpm", "dev", "--host", "0.0.0.0", "--port", "4002"]
