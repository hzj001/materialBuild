# 建材通 Docker 环境变量
# 复制为 .env 后修改: cp .env.example .env

# MySQL
MYSQL_ROOT_PASSWORD=materialbuild123
MYSQL_PASSWORD=materialbuild123
MYSQL_PORT=4306

# Redis
REDIS_PORT=4379

# 服务端口（均以 400 开头，避免与本机其他服务冲突）
BACKEND_PORT=4008
CLIENT_PORT=4000
MERCHANT_PORT=4001
ADMIN_PORT=4002
